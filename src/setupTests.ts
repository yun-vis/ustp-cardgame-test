
// src/setupTests.ts
import '@testing-library/jest-dom'

// // src/setupTests.ts  (or src/setupTest.ts — match your vite.config.ts)

// import { expect as vitestExpect } from 'vitest';

// // Attach vitest's expect to the global object before loading jest-dom.
// // This prevents "ReferenceError: expect is not defined" when jest-dom runs.
// ;(globalThis as any).expect = vitestExpect;

// // Now import jest-dom (it will find global expect).
// import '@testing-library/jest-dom';
